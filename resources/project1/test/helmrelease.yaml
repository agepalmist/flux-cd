apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test
  namespace: flux-system
spec:
  interval: 5m
  releaseName: test
  chart:
    spec:
      chart: test
      sourceRef:
        kind: GitRepository
        name: fluxcd-helm-charts
