apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: fluxcd-helm-charts
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: project1
  secretRef:
    name: fluxcd-helm-charts
  url: ssh://git@github.com/agepalmist/flux-cd.git